# Environment
.env
venv/
.venv/
__pycache__/
*.pyc

# Data files — never commit raw or processed data
data/

# Outputs — exports are generated, not source; figures are committed
outputs/exports/

# Project-specific — tasks are internal
tasks/
docs/tableau_viz_guide.md

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/

# Node — used locally to screenshot the Tableau dashboard via Puppeteer, not part of the Python pipeline
node_modules/
package.json
package-lock.json
